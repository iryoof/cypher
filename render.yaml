services:
  - type: web
    name: cypher-backend
    env: node
    region: frankfurt
    plan: free
    buildCommand: npm install && npm run build --workspace=backend
    startCommand: npm start --workspace=backend
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      # Comma-separated list of allowed browser origins (no trailing slash, no path).
      # First entry is the live GitHub Pages site, second is the local Vite dev server.
      - key: FRONTEND_URL
        value: https://iryoof.github.io,http://localhost:5173

# No database: all lobby/game state is kept in memory in the backend process,
# archives are written to the local filesystem by saveManager.ts.
